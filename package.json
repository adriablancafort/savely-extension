{
  "name": "pricecomparison-extension",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "build:external": "vite build --config vite.external.config.js",
    "package": "npm run build && zip -r package.zip package/"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^5.0.3",
    "svelte": "^5.32.1",
    "vite": "^6.3.5"
  }
}
